// The following is a config using project references.
// We can't use it yet as long as we have `allowJs` enabled.
// https://www.typescriptlang.org/docs/handbook/project-references.html
// {
//   "references": [
//     { "path": "packages/react-data-grid" },
//     { "path": "packages/react-data-grid-addons" }
//   ]
// }

// Temporary config to get types working in vscode:
{
  "extends": "./tsconfig.base",
  "compilerOptions": {
    "noEmit": true
  },
  "files": [
    "packages/react-data-grid/src/index.ts",
    "packages/react-data-grid-addons/src/index.ts"
  ]
}
