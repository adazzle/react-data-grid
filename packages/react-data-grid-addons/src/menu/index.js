import { ContextMenu, MenuItem, SubMenu, ContextMenuTrigger } from 'react-contextmenu';
import MenuHeader from './MenuHeader';

const menu = {
    ContextMenu,
    MenuHeader,
    MenuItem,
    SubMenu,
    ContextMenuTrigger
};

export default menu;
export { ContextMenu, MenuHeader, MenuItem, SubMenu, ContextMenuTrigger };
